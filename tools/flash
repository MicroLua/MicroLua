#!/bin/bash

if [[ $# != 1 ]]; then
    echo "Usage: flash BINARY.elf" >&2
    exit 1
fi

openocd -f interface/cmsis-dap.cfg -f target/rp2040.cfg \
    -c "adapter speed 5000" \
    -c "program $1 verify reset exit"
